
Downloaded from http://www.vinartus.net/spa/scol-1-12.tgz

To attempt to build the CASS program:

   sudo tar zxvf  scol-1-12.tgz
   sudo chown -R www-data: scol-1-12

Otherwise, use the bin catalogue

   sudo chown -R www-data: bin
   sudo chmod ugo+x bin/*
